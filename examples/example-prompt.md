# Example prompt

Once the template files are installed, keep normal prompts focused on the actual task.

```text
Add passwordless login using magic links.

Keep the existing auth architecture and email provider.
Do not change the existing password login flow.
Done when the flow works end-to-end and the relevant tests pass.
```

The persistent orchestration configuration handles planning, decomposition, worker routing, review and final acceptance.
